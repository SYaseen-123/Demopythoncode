import sys
#sys is a package
print(sys.version)

#numpy and pyodbc to install these we use pip install
#socket used by programming language --logical socket

import socket #socket is creating the connection
#socket--core package
hostname=socket.gethostname()
print(hostname)
ipadd=socket.gethostbyname(hostname)
print(ipadd)
"""
python-Dynamic+oop
oop-Abtraction
exposing:gethostname()-->socket
LOC lesser--MoreAgile it is::Agile/Scrum
more LOC-morebugs--more issues--more time
"""


